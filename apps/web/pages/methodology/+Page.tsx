import { Alert, Card, Typography } from 'antd';
import type { MethodologyData } from './+data.js';

const { Title, Paragraph, Text } = Typography;

/** `/methodology` — см. doc-комментарий +data.ts. Реальные факты о ядре (не выдумка): источники
 *  алгоритмов сверены с docs/architecture/21-техническая-архитектура.md §3 и packages/astro-core/
 *  ACCURACY.md на момент написания. */
export function Page({ pageContext }: { pageContext: { data: MethodologyData } }): React.JSX.Element {
  const { coreVersion } = pageContext.data;

  return (
    <main style={{ maxWidth: 760, margin: '48px auto', padding: '0 24px 64px' }}>
      <Title>Методология расчётов</Title>
      <Paragraph>
        Расчётное ядро Зодиакум (версия <Text code>{coreVersion}</Text>) — набор детерминированных
        функций без искусственного интеллекта: одни и те же входные данные всегда дают один и тот
        же результат, воспроизводимый и проверяемый.
      </Paragraph>

      <Card style={{ marginTop: 24, marginBottom: 16 }}>
        <Title level={3}>Источники алгоритмов</Title>
        <Paragraph>
          Позиции Солнца, Луны и планет от Меркурия до Плутона считаются через открытую библиотеку{' '}
          <Text code>astronomy-engine</Text> (геоцентрическая видимая эклиптическая долгота эпохи
          даты — с учётом нутации, аберрации и светового времени). Для Солнца в задачах повышенной
          точности (соляры) дополнительно используются ряды VSOP87. Позиция Хирона — по
          предрассчитанной таблице на основе данных JPL Horizons (1900–2100 гг.) с чебышёвской
          интерполяцией. Системы домов (Плацидус, Кох, Регимонтан, Порфирий, равнодомная, целознаковая),
          аспекты, вторичные прогрессии, символические дирекции, композит и карта Дэвисон — по
          классическим формулам сферической астрономии («Astronomical Algorithms», Жан Меёс).
        </Paragraph>
        <Paragraph>
          На платные лицензированные эфемериды (Swiss Ephemeris) сервис принципиально не опирается —
          используются только открытые источники.
        </Paragraph>
      </Card>

      <Card style={{ marginBottom: 16 }}>
        <Title level={3}>Точность и допущения</Title>
        <Paragraph>
          Ядро проходит регрессионное тестирование против независимого открытого эфемеридного
          источника на широком диапазоне дат (1900–2050) и против вручную сверенных эталонных карт.
          Известные упрощения — например, система домов автоматически заменяется на Порфирия для
          широт свыше 66,5° (полярный круг, где Плацидус математически не определён), а при
          неизвестном времени рождения дома и асцендент не рассчитываются вовсе, вместо того чтобы
          подставлять предполагаемое время.
        </Paragraph>
      </Card>

      <Card style={{ marginBottom: 16 }}>
        <Title level={3}>Элективные окна (поиск благоприятной даты)</Title>
        <Paragraph>
          Индивидуальный прогноз «поиск благоприятного окна» сканирует заданный интервал дат и
          оценивает каждый момент по прозрачной формуле: транзитные аспекты к натальным планетам
          (гармоничные — трин, секстиль — считаются благоприятными; напряжённые — квадрат,
          оппозиция — сложными; соединение — нейтрально, его природа не оценивается автоматически),
          с учётом периода «Луна без курса» (void-of-course) и, по запросу, ретроградного Меркурия.
          Веса планет и орбисы — те же, что используются во всех остальных расчётах сервиса, без
          отдельной скрытой эвристики.
        </Paragraph>
      </Card>

      <Alert
        type="info"
        showIcon
        message="Разбор текста — с участием ИИ, расчёт дат и позиций — нет"
        description="Интерпретации формулируются с помощью ИИ-провайдера на основе уже вычисленных фактов карты — ИИ не подбирает и не угадывает позиции планет и даты, они приходят к нему уже готовыми."
      />
    </main>
  );
}
