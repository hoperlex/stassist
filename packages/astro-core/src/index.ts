/**
 * packages/astro-core — расчётное ядро (чистый TS, без I/O): позиции планет, дома, аспекты,
 * прогностика (см. docs/architecture/21-техническая-архитектура.md §3).
 *
 * В Ф0 пакет — пустой каркас: реализация появится в Ф1 вместе с golden-тестами
 * (docs/roadmap/prompts/f1-расчетное-ядро.md).
 */

/** Версия ядра — попадёт в ChartData.meta в Ф1, чтобы каждый ИИ-отчёт знал, «как считали». */
export const ASTRO_CORE_VERSION = '0.0.0-f0-placeholder';
