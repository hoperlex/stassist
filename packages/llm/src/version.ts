/**
 * Версии, зашиваемые в `ai_reports`/`interpretation_chunks` для аудита и инвалидации кэша (см.
 * packages/llm/src/cache/cache-key.ts, docs/architecture/22-модель-данных.md §6 «чанки
 * версионируются — отчёт хранит версии использованных фрагментов»).
 */

/** Версия сборки промта (системные правила + структура задачи) — см. src/prompt/build-prompt.ts.
 *  Бампается при ЛЮБОМ смысловом изменении системного промта/структуры ответа: старые cache_key
 *  перестают совпадать → отчёт перегенерируется, а не отдаётся протухшим из кэша. */
export const PROMPT_VERSION = 'f4-2026.07.2';

/** Версия seed-корпуса (см. tools/gen-corpus.ts) — бампается при регенерации корпуса с иным
 *  содержимым/наполнением ключей, чтобы можно было отличить, каким корпусом сгенерирован отчёт. */
export const CORPUS_VERSION = 'f4-corpus-2026.07.1';
